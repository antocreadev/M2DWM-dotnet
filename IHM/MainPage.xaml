<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="IHM.MainPage">

    <ScrollView>
        <VerticalStackLayout
            Padding="30,0"
            Spacing="25">
            <Image
                Source="dotnet_bot.png"
                HeightRequest="185"
                Aspect="AspectFit"
                SemanticProperties.Description="dot net bot in a hovercraft number nine" />

            <Label
                Text="Recipes App"
                Style="{StaticResource Headline}"
                SemanticProperties.HeadingLevel="Level1" />

            <CollectionView x:Name="RecipesCollectionView"
                            ItemsSource="{Binding Recipes}"
                            SelectionMode="None">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Grid Padding="10">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <Label Grid.Row="0" Grid.Column="0" Text="{Binding Name}" FontSize="18" FontAttributes="Bold" />
                            <Label Grid.Row="1" Grid.Column="0" Text="{Binding Instructions}" FontSize="14" />
                            <Label Grid.Row="2" Grid.Column="0" Text="{Binding Ingredients, StringFormat='Ingredients: {0}'}" FontSize="12" />
                            <Button Grid.Row="0" Grid.Column="1" Text="✏️" BackgroundColor="Transparent" Clicked="OnEditRecipeClicked" />
                            <Button Grid.Row="0" Grid.Column="2" Text="❌" BackgroundColor="Transparent" Clicked="OnDeleteRecipeClicked" />
                        </Grid>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

            <Label Text="Add New Recipe" FontSize="16" FontAttributes="Bold" />

            <Entry x:Name="RecipeNameEntry" Placeholder="Recipe Name" />
            <Entry x:Name="IngredientsEntry" Placeholder="Ingredients (comma separated)" />
            <Entry x:Name="InstructionsEntry" Placeholder="Instructions" />
            <Entry x:Name="PrepTimeEntry" Placeholder="Prep Time (minutes)" Keyboard="Numeric" />
            <Entry x:Name="CookTimeEntry" Placeholder="Cook Time (minutes)" Keyboard="Numeric" />

            <Button
                x:Name="AddRecipeBtn"
                Text="Add Recipe"
                Clicked="OnAddRecipeClicked"
                HorizontalOptions="Fill" />
        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
